# Agent/AI tool configs
.agents/
.claude/
.codex/
.mcp.json
skills-lock.json
.env
.claude
.env.example

# dependencies (any nested node_modules across the repo)
node_modules/
# Certificate name lists — participant names and college emails. Generated from
# the live database, never source. Regenerate rather than commit.
/certificates/
