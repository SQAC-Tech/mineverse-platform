# Round 3

Phase 2 feature folder. Round 3 gameplay UI and logic live here:

- Components for the round's challenge interface
- Round-specific hooks / state
- Any round-only utilities

Pages stay under `app/` (Next.js routing) and should import from this folder,
same as `features/dashboard` and `features/landing-registration`.
